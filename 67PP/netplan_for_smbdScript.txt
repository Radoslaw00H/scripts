lines 187-260

example 01 or 50 netplan yaml file:

"

network:
    version: 2
    renderer: networkd
    ethernets:
        enp3s0:
            dhcp4: true
            addresses: [192.168.0.1/24]

"
CHECK interface name first check if its enp3s0 or enp0s2 (in the current /etc/netplan/ file) + use ip a to determine it if nesesary
PREFER 50-netplan... yaml file not the 01 one but if the 50 is unavaible use 01 if 01 is unavaible use 50 and prefer 50 tell the user in polish that if it doesnt work then use rm and whatever is nesesary to remove the 01 file
APPLY at the end

you will change
- ip adress and mask to the users typed in
- dhcp to whatever you choose true or false (the use dhcp option [T/N] - t makes dhcp on (tak) and n makes dhcp off (nie) - in polish)
